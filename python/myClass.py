#!/usr/bin/python
# filename:myClass.py


class myClass:
    'I write this class to try the features of python classes'
    count = 0  # definition and initial assignation is neccessary

    def __init__(self, num):
        'initialize the domain and count the instances'
        myClass.count += 1
        self.number = num
        self.__number = num+1

    def PrintCount(self):
        'this function is designed to print the number of the instances'
        print' the number of instances is:', myClass.count

    def PrintNumber(self):
        'print the domain of this instance'
        print' from this instance, number is:', self.number
        print' from this instance, __number is:', self.__number


myObj1 = myClass(3)
myObj1.PrintCount()

myObj2.PrintCount()

print myObj1.count
print myObj2.count
print myClass.count

myObj1.PrintNumber()

print myObj1.__number
#!/usr/bin/python
# filename:myClass.py


class myClass:
    'I write this class to try the features of python classes'
    count = 0  # definition and initial assignation is neccessary

    def __init__(self, num):
        'initialize the domain and count the instances'
        myClass.count += 1
        self.number = num
        self.__number = num + 1

    def PrintCount(self):
        'this function is designed to print the number of the instances'
        print ' the number of instances is:', myClass.count

    def PrintNumber(self):
        'print the domain of this instance'
        print ' from this instance, number is:', self.number
        print ' from this instance, __number is:', self.__number


myObj1 = myClass(3)
myObj1.PrintCount()

myObj2.PrintCount()

print myObj1.count
print myObj2.count
print myClass.count

myObj1.PrintNumber()

print myObj1.__number
#!/usr/bin/python
# filename:myClass.py


class myClass:
    'I write this class to try the features of python classes'
    count = 0  # definition and initial assignation is neccessary

    def __init__(self, num):
        'initialize the domain and count the instances'
        myClass.count += 1
        self.number = num
        self.__number = num + 1

    def PrintCount(self):
        'this function is designed to print the number of the instances'
        print ' the number of instances is:', myClass.count

    def PrintNumber(self):
        'print the domain of this instance'
        print ' from this instance, number is:', self.number
        print ' from this instance, __number is:', self.__number


myObj1 = myClass(3)
myObj1.PrintCount()

myObj2.PrintCount()

print myObj1.count
print myObj2.count
print myClass.count

myObj1.PrintNumber()

print myObj1.__number

#!/usr/bin/python
# filename:myClass.py


class myClass:
    'I write this class to try the features of python classes'
    count = 0  # definition and initial assignation is neccessary

    def __init__(self, num):
        'initialize the domain and count the instances'
        myClass.count += 1
        self.number = num
        self.__number = num + 1

    def PrintCount(self):
        'this function is designed to print the number of the instances'
        print ' the number of instances is:', myClass.count

    def PrintNumber(self):
        'print the domain of this instance'
        print ' from this instance, number is:', self.number
        print ' from this instance, __number is:', self.__number


myObj1 = myClass(3)
myObj1.PrintCount()

myObj2.PrintCount()

print myObj1.count
print myObj2.count
print myClass.count

myObj1.PrintNumber()

print myObj1.__number
#!/usr/bin/python
# filename:myClass.py


class myClass:
    'I write this class to try the features of python classes'
    count = 0  # definition and initial assignation is neccessary

    def __init__(self, num):
        'initialize the domain and count the instances'
        myClass.count += 1
        self.number = num
        self.__number = num + 1

    def PrintCount(self):
        'this function is designed to print the number of the instances'
        print ' the number of instances is:', myClass.count

    def PrintNumber(self):
        'print the domain of this instance'
        print ' from this instance, number is:', self.number
        print ' from this instance, __number is:', self.__number


myObj1 = myClass(3)
myObj1.PrintCount()

myObj2.PrintCount()

print myObj1.count
print myObj2.count
print myClass.count

myObj1.PrintNumber()

print myObj1.__number

#!/usr/bin/python
# filename:myClass.py


class myClass:
    'I write this class to try the features of python classes'
    count = 0  # definition and initial assignation is neccessary

    def __init__(self, num):
        'initialize the domain and count the instances'
        myClass.count += 1
        self.number = num
        self.__number = num + 1

    def PrintCount(self):
        'this function is designed to print the number of the instances'
        print ' the number of instances is:', myClass.count

    def PrintNumber(self):
        'print the domain of this instance'
        print ' from this instance, number is:', self.number
        print ' from this instance, __number is:', self.__number


myObj1 = myClass(3)
myObj1.PrintCount()

myObj2.PrintCount()

print myObj1.count
print myObj2.count
print myClass.count

myObj1.PrintNumber()

print myObj1.__number

#!/usr/bin/python
# filename:myClass.py


class myClass:
    'I write this class to try the features of python classes'
    count = 0  # definition and initial assignation is neccessary

    def __init__(self, num):
        'initialize the domain and count the instances'
        myClass.count += 1
        self.number = num
        self.__number = num + 1

    def PrintCount(self):
        'this function is designed to print the number of the instances'
        print ' the number of instances is:', myClass.count

    def PrintNumber(self):
        'print the domain of this instance'
        print ' from this instance, number is:', self.number
        print ' from this instance, __number is:', self.__number


myObj1 = myClass(3)
myObj1.PrintCount()

myObj2.PrintCount()

print myObj1.count
print myObj2.count
print myClass.count

myObj1.PrintNumber()

print myObj1.__number

#!/usr/bin/python
# filename:myClass.py


class myClass:
    'I write this class to try the features of python classes'
    count = 0  # definition and initial assignation is neccessary

    def __init__(self, num):
        'initialize the domain and count the instances'
        myClass.count += 1
        self.number = num
        self.__number = num + 1

    def PrintCount(self):
        'this function is designed to print the number of the instances'
        print ' the number of instances is:', myClass.count

    def PrintNumber(self):
        'print the domain of this instance'
        print ' from this instance, number is:', self.number
        print ' from this instance, __number is:', self.__number


myObj1 = myClass(3)
myObj1.PrintCount()

myObj2.PrintCount()

print myObj1.count
print myObj2.count
print myClass.count

myObj1.PrintNumber()

print myObj1.__number
